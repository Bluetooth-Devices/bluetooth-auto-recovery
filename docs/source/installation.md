# Installation

The package is published on [PyPI](https://pypi.org/project/bluetooth-auto-recovery/) and can be installed with `pip` (or any equivalent):

```bash
pip install bluetooth-auto-recovery
```
